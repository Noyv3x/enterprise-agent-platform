diff --git a/tools/environments/local.py b/tools/environments/local.py
index df045e576..e81087990 100644
--- a/tools/environments/local.py
+++ b/tools/environments/local.py
@@ -288,5 +288,16 @@ def _is_hermes_internal_secret(key: str) -> bool:
     if upper.startswith("GATEWAY_RELAY_") and (
         upper.endswith("_SECRET") or upper.endswith("_KEY") or upper.endswith("_TOKEN")
     ):
         return True
+    if upper.startswith("ENTERPRISE_") and upper.endswith(
+        ("_SECRET", "_TOKEN", "_PASSWORD", "_API_KEY")
+    ):
+        return True
+    if upper in {
+        "CAMOFOX_ACCESS_KEY",
+        "CAMOFOX_API_KEY",
+        "CAMOFOX_ADMIN_KEY",
+        "API_SERVER_KEY",
+    }:
+        return True
     return False

diff --git a/agent/agent_init.py b/agent/agent_init.py
index f1666a1aa..72ffb9a99 100644
--- a/agent/agent_init.py
+++ b/agent/agent_init.py
@@ -401,6 +401,7 @@ def init_agent(
     agent._chat_type = chat_type
     agent._thread_id = thread_id
     agent._gateway_session_key = gateway_session_key  # Stable per-chat key (e.g. agent:main:telegram:dm:123)
+    agent._shares_parent_terminal_scope = False
     # Pluggable print function — CLI replaces this with _cprint so that
     # raw ANSI status lines are routed through prompt_toolkit's renderer
     # instead of going directly to stdout where patch_stdout's StdoutProxy
@@ -1341,6 +1342,7 @@ def init_agent(
                 agent._memory_store = MemoryStore(
                     memory_char_limit=mem_config.get("memory_char_limit", 2200),
                     user_char_limit=mem_config.get("user_char_limit", 1375),
+                    memory_scope_key=agent._gateway_session_key,
                 )
                 agent._memory_store.load_from_disk()
         except Exception:
@@ -1363,6 +1365,7 @@ def init_agent(
                 if _mp and _mp.is_available():
                     agent._memory_manager.add_provider(_mp)
                 if agent._memory_manager.providers:
+                    _agent_scope_key = str(agent._gateway_session_key or "").strip()
                     _init_kwargs = {
                         "session_id": agent.session_id,
                         "platform": platform or "cli",
@@ -1397,8 +1400,13 @@ def init_agent(
                     if agent._thread_id:
                         _init_kwargs["thread_id"] = agent._thread_id
                     # Thread gateway session key for stable per-chat Honcho session isolation
-                    if agent._gateway_session_key:
-                        _init_kwargs["gateway_session_key"] = agent._gateway_session_key
+                    if _agent_scope_key:
+                        _init_kwargs["gateway_session_key"] = _agent_scope_key
+                        _init_kwargs["agent_scope_key"] = _agent_scope_key
+                        # Providers that scope by user/agent id should not
+                        # collapse all API-server agents into one default user.
+                        _init_kwargs.setdefault("user_id", _agent_scope_key)
+                        _init_kwargs.setdefault("agent_id", _agent_scope_key)
                     # Profile identity for per-profile provider scoping
                     try:
                         from hermes_cli.profiles import get_active_profile_name
@@ -1942,8 +1950,14 @@ def init_agent(
         except Exception as _ce_err:
             _ra().logger.debug("Context engine on_session_start: %s", _ce_err)
 
+    try:
+        from agent.runtime_cwd import resolve_context_cwd
+
+        _context_working_dir = resolve_context_cwd()
+    except Exception:
+        _context_working_dir = None
     agent._subdirectory_hints = SubdirectoryHintTracker(
-        working_dir=os.getenv("TERMINAL_CWD") or None,
+        working_dir=str(_context_working_dir) if _context_working_dir else (os.getenv("TERMINAL_CWD") or None),
     )
     agent._user_turn_count = 0
     # Copilot x-initiator flag: first API call of a user turn sends "user" (#3040).
diff --git a/agent/agent_runtime_helpers.py b/agent/agent_runtime_helpers.py
index 3e5cd6353..6e9b88ab1 100644
--- a/agent/agent_runtime_helpers.py
+++ b/agent/agent_runtime_helpers.py
@@ -2216,6 +2216,7 @@ def invoke_tool(agent, function_name: str, function_args: dict, effective_task_i
                     sort=next_args.get("sort"),
                     db=session_db,
                     current_session_id=agent.session_id,
+                    current_session_key=getattr(agent, "_gateway_session_key", None),
                 ),
                 next_args,
             )
@@ -2287,6 +2288,7 @@ def invoke_tool(agent, function_name: str, function_args: dict, effective_task_i
                 enabled_toolsets=getattr(agent, "enabled_toolsets", None),
                 disabled_toolsets=getattr(agent, "disabled_toolsets", None),
                 tool_request_middleware_trace=list(_tool_middleware_trace),
+                agent_scope_key=getattr(agent, "_gateway_session_key", None),
             )
 
     from hermes_cli.middleware import run_tool_execution_middleware
diff --git a/agent/background_review.py b/agent/background_review.py
index 3f4e5efcd..ef221c48e 100644
--- a/agent/background_review.py
+++ b/agent/background_review.py
@@ -594,6 +594,7 @@ def build_memory_write_metadata(
         ),
         "session_id": agent.session_id or "",
         "parent_session_id": agent._parent_session_id or "",
+        "gateway_session_key": getattr(agent, "_gateway_session_key", "") or "",
         "platform": agent.platform or os.environ.get("HERMES_SESSION_SOURCE", "cli"),
         "tool_name": "memory",
     }
diff --git a/agent/chat_completion_helpers.py b/agent/chat_completion_helpers.py
index c3df449bd..4c0bd167d 100644
--- a/agent/chat_completion_helpers.py
+++ b/agent/chat_completion_helpers.py
@@ -1817,18 +1817,32 @@ def cleanup_task_resources(agent, task_id: str) -> None:
     torn down per-turn as before to prevent resource leakage (the original
     intent of this hook for the Morph backend, see commit fbd3a2fd).
     """
-    try:
-        if is_persistent_env(task_id):
+    terminal_task_id = task_id
+    gateway_session_key = getattr(agent, "_gateway_session_key", None)
+    if gateway_session_key:
+        try:
+            from tools.terminal_tool import _resolve_container_task_id
+
+            terminal_task_id = _resolve_container_task_id(
+                task_id,
+                gateway_session_key,
+            )
+        except Exception:
+            pass
+
+    if not getattr(agent, "_shares_parent_terminal_scope", False):
+        try:
+            if is_persistent_env(terminal_task_id):
+                if agent.verbose_logging:
+                    logging.debug(
+                        f"Skipping per-turn cleanup_vm for persistent env {terminal_task_id}; "
+                        f"idle reaper will handle it."
+                    )
+            else:
+                _ra().cleanup_vm(terminal_task_id)
+        except Exception as e:
             if agent.verbose_logging:
-                logging.debug(
-                    f"Skipping per-turn cleanup_vm for persistent env {task_id}; "
-                    f"idle reaper will handle it."
-                )
-        else:
-            _ra().cleanup_vm(task_id)
-    except Exception as e:
-        if agent.verbose_logging:
-            logger.warning(f"Failed to cleanup VM for task {task_id}: {e}")
+                logger.warning(f"Failed to cleanup VM for task {terminal_task_id}: {e}")
     try:
         _ra().cleanup_browser(task_id)
     except Exception as e:
diff --git a/agent/conversation_compression.py b/agent/conversation_compression.py
index 843960cc2..746b997bb 100644
--- a/agent/conversation_compression.py
+++ b/agent/conversation_compression.py
@@ -786,12 +786,21 @@ def compress_context(
                         pass
                     agent._session_db_created = False
                     try:
+                        try:
+                            from agent.runtime_cwd import resolve_context_cwd
+
+                            _context_cwd = resolve_context_cwd()
+                            _session_cwd = str(_context_cwd) if _context_cwd else None
+                        except Exception:
+                            _session_cwd = None
                         agent._session_db.create_session(
                             session_id=agent.session_id,
                             source=agent.platform or os.environ.get("HERMES_SESSION_SOURCE", "cli"),
                             model=agent.model,
                             model_config=agent._session_init_model_config,
+                            session_key=getattr(agent, "_gateway_session_key", None),
                             parent_session_id=old_session_id,
+                            cwd=_session_cwd,
                         )
                     except Exception as _cs_err:
                         # The child row could not be created (e.g. FK constraint,
diff --git a/agent/prompt_builder.py b/agent/prompt_builder.py
index b5b2b58c3..c017508d9 100644
--- a/agent/prompt_builder.py
+++ b/agent/prompt_builder.py
@@ -935,7 +935,13 @@ def _probe_remote_backend(env_type: str) -> str | None:
     per process. Used only for non-local backends where the agent's tools
     operate on a different machine than the host Hermes runs on.
     """
-    cwd_hint = os.getenv("TERMINAL_CWD", "")
+    try:
+        from agent.runtime_cwd import resolve_context_cwd
+
+        context_cwd = resolve_context_cwd()
+        cwd_hint = str(context_cwd) if context_cwd else os.getenv("TERMINAL_CWD", "")
+    except Exception:
+        cwd_hint = os.getenv("TERMINAL_CWD", "")
     cache_key = (env_type, cwd_hint)
     cached = _BACKEND_PROBE_CACHE.get(cache_key)
     if cached is not None:
diff --git a/agent/tool_executor.py b/agent/tool_executor.py
index 9688f5d3d..bc20dfada 100644
--- a/agent/tool_executor.py
+++ b/agent/tool_executor.py
@@ -478,7 +478,12 @@ def execute_tool_calls_concurrent(agent, assistant_message, messages: list, effe
                 try:
                     cmd = function_args.get("command", "")
                     if _is_destructive_command(cmd):
-                        cwd = function_args.get("workdir") or os.getenv("TERMINAL_CWD", os.getcwd())
+                        if function_args.get("workdir"):
+                            cwd = function_args["workdir"]
+                        else:
+                            from agent.runtime_cwd import resolve_agent_cwd
+
+                            cwd = str(resolve_agent_cwd())
                         agent._checkpoint_mgr.ensure_checkpoint(
                             cwd, f"before terminal: {cmd[:60]}"
                         )
@@ -1122,7 +1127,12 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
             try:
                 cmd = function_args.get("command", "")
                 if _is_destructive_command(cmd):
-                    cwd = function_args.get("workdir") or os.getenv("TERMINAL_CWD", os.getcwd())
+                    if function_args.get("workdir"):
+                        cwd = function_args["workdir"]
+                    else:
+                        from agent.runtime_cwd import resolve_agent_cwd
+
+                        cwd = str(resolve_agent_cwd())
                     agent._checkpoint_mgr.ensure_checkpoint(
                         cwd, f"before terminal: {cmd[:60]}"
                     )
@@ -1200,6 +1210,7 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
                     sort=next_args.get("sort"),
                     db=session_db,
                     current_session_id=agent.session_id,
+                    current_session_key=getattr(agent, "_gateway_session_key", None),
                 )
             function_result, function_args = _run_agent_tool_execution_middleware(
                 agent,
@@ -1415,6 +1426,7 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
                     enabled_toolsets=getattr(agent, "enabled_toolsets", None),
                     disabled_toolsets=getattr(agent, "disabled_toolsets", None),
                     tool_request_middleware_trace=list(middleware_trace),
+                    agent_scope_key=getattr(agent, "_gateway_session_key", None),
                 )
                 _spinner_result = function_result
             except KeyboardInterrupt:
@@ -1457,6 +1469,7 @@ def execute_tool_calls_sequential(agent, assistant_message, messages: list, effe
                     enabled_toolsets=getattr(agent, "enabled_toolsets", None),
                     disabled_toolsets=getattr(agent, "disabled_toolsets", None),
                     tool_request_middleware_trace=list(middleware_trace),
+                    agent_scope_key=getattr(agent, "_gateway_session_key", None),
                 )
             except KeyboardInterrupt:
                 _emit_cancelled_terminal_post_tool_call(
diff --git a/gateway/platforms/api_server.py b/gateway/platforms/api_server.py
index 5ba09d674..ee06afd1e 100644
--- a/gateway/platforms/api_server.py
+++ b/gateway/platforms/api_server.py
@@ -1137,6 +1137,35 @@ class APIServerAdapter(BasePlatformAdapter):
 
         return raw, None
 
+    @staticmethod
+    def _workspace_cwd_from_body(body: Any) -> str:
+        """Return a request-scoped workspace cwd from safe metadata, if present."""
+        if not isinstance(body, dict):
+            return ""
+        metadata = body.get("metadata")
+        if not isinstance(metadata, dict):
+            return ""
+
+        candidates: List[Any] = [metadata.get("workspace_path")]
+        workspace = metadata.get("workspace")
+        if isinstance(workspace, dict):
+            candidates.append(workspace.get("path"))
+        container = metadata.get("container")
+        if isinstance(container, dict):
+            candidates.append(container.get("workspace_path"))
+
+        for raw in candidates:
+            value = str(raw or "").strip()
+            if not value or re.search(r"[\r\n\x00]", value):
+                continue
+            try:
+                path = Path(value).expanduser()
+                if path.is_absolute() and path.is_dir():
+                    return str(path.resolve())
+            except Exception:
+                continue
+        return ""
+
     # ------------------------------------------------------------------
     # Session DB helper
     # ------------------------------------------------------------------
@@ -1894,6 +1923,7 @@ class APIServerAdapter(BasePlatformAdapter):
         system_prompt = body.get("system_message") or body.get("instructions")
         if system_prompt is not None and not isinstance(system_prompt, str):
             return web.json_response(_openai_error("system_message must be a string", code="invalid_system_message"), status=400)
+        session_cwd = self._workspace_cwd_from_body(body)
         history = self._conversation_history_for_session(session_id)
         result, usage = await self._run_agent(
             user_message=user_message,
@@ -1901,6 +1931,7 @@ class APIServerAdapter(BasePlatformAdapter):
             ephemeral_system_prompt=system_prompt,
             session_id=session_id,
             gateway_session_key=gateway_session_key,
+            session_cwd=session_cwd,
         )
         effective_session_id = result.get("session_id") if isinstance(result, dict) else session_id
         final_response = _resolve_media_to_data_urls(result.get("final_response", "") if isinstance(result, dict) else "")
@@ -1938,6 +1969,7 @@ class APIServerAdapter(BasePlatformAdapter):
         system_prompt = body.get("system_message") or body.get("instructions")
         if system_prompt is not None and not isinstance(system_prompt, str):
             return web.json_response(_openai_error("system_message must be a string", code="invalid_system_message"), status=400)
+        session_cwd = self._workspace_cwd_from_body(body)
 
         loop = asyncio.get_running_loop()
         queue: "asyncio.Queue[Optional[tuple[str, Dict[str, Any]]]]" = asyncio.Queue()
@@ -1992,6 +2024,7 @@ class APIServerAdapter(BasePlatformAdapter):
                     stream_delta_callback=_delta,
                     tool_progress_callback=_tool_progress,
                     gateway_session_key=gateway_session_key,
+                    session_cwd=session_cwd,
                 )
                 final_response = _resolve_media_to_data_urls(result.get("final_response", "") if isinstance(result, dict) else "")
                 effective_session_id = result.get("session_id", session_id) if isinstance(result, dict) else session_id
@@ -2187,6 +2220,7 @@ class APIServerAdapter(BasePlatformAdapter):
             session_id = _derive_chat_session_id(system_prompt, first_user)
             # history already set from request body above
 
+        session_cwd = self._workspace_cwd_from_body(body)
         completion_id = f"chatcmpl-{uuid.uuid4().hex[:29]}"
         model_name = body.get("model", self._model_name)
         created = int(time.time())
@@ -2279,6 +2313,7 @@ class APIServerAdapter(BasePlatformAdapter):
                 agent_ref=agent_ref,
                 gateway_session_key=gateway_session_key,
                 route=route,
+                session_cwd=session_cwd,
             ))
             # Ensure SSE drain loops can terminate without relying on polling
             # agent_task.done(), which can race with queue timeout checks.
@@ -2299,6 +2334,7 @@ class APIServerAdapter(BasePlatformAdapter):
                 session_id=session_id,
                 gateway_session_key=gateway_session_key,
                 route=route,
+                session_cwd=session_cwd,
             )
 
         idempotency_key = request.headers.get("Idempotency-Key")
@@ -3308,6 +3344,7 @@ class APIServerAdapter(BasePlatformAdapter):
         # Reuse session from previous_response_id chain so the dashboard
         # groups the entire conversation under one session entry.
         session_id = stored_session_id or str(uuid.uuid4())
+        session_cwd = self._workspace_cwd_from_body(body)
 
         # Per-client model routing for /v1/responses (see model_routes).
         route = self._resolve_route(body.get("model"))
@@ -3366,6 +3403,7 @@ class APIServerAdapter(BasePlatformAdapter):
                 agent_ref=agent_ref,
                 gateway_session_key=gateway_session_key,
                 route=route,
+                session_cwd=session_cwd,
             ))
             # Ensure SSE drain loops can terminate without relying on polling
             # agent_task.done(), which can race with queue timeout checks.
@@ -3400,6 +3438,7 @@ class APIServerAdapter(BasePlatformAdapter):
                 session_id=session_id,
                 gateway_session_key=gateway_session_key,
                 route=route,
+                session_cwd=session_cwd,
             )
 
         idempotency_key = request.headers.get("Idempotency-Key")
@@ -3992,6 +4031,7 @@ class APIServerAdapter(BasePlatformAdapter):
         chat_id: str = "",
         session_key: str = "",
         session_id: str = "",
+        cwd: str = "",
     ) -> list:
         """Bind session contextvars for an API-server agent run.
 
@@ -4015,6 +4055,7 @@ class APIServerAdapter(BasePlatformAdapter):
             chat_id=chat_id,
             session_key=session_key,
             session_id=session_id,
+            cwd=cwd,
             async_delivery=False,
         )
 
@@ -4031,6 +4072,7 @@ class APIServerAdapter(BasePlatformAdapter):
         agent_ref: Optional[list] = None,
         gateway_session_key: Optional[str] = None,
         route: Optional[Dict[str, Any]] = None,
+        session_cwd: Optional[str] = None,
     ) -> tuple:
         """
         Create an agent and run a conversation in a thread executor.
@@ -4056,6 +4098,7 @@ class APIServerAdapter(BasePlatformAdapter):
                 chat_id=session_id or "",
                 session_key=gateway_session_key or session_id or "",
                 session_id=session_id or "",
+                cwd=session_cwd or "",
             )
             try:
                 agent = self._create_agent(
@@ -4243,13 +4286,38 @@ class APIServerAdapter(BasePlatformAdapter):
                     conversation_history.append({"role": msg["role"], "content": str(content)})
 
         run_id = f"run_{uuid.uuid4().hex}"
-        session_id = body.get("session_id") or stored_session_id or run_id
+        requested_session_id = body.get("session_id")
+        if requested_session_id is not None:
+            if not isinstance(requested_session_id, str):
+                return web.json_response(
+                    _openai_error("Invalid session ID", code="invalid_session_id"),
+                    status=400,
+                )
+            requested_session_id = requested_session_id.strip()
+            from gateway.session import _is_path_unsafe
+
+            if (
+                not requested_session_id
+                or re.search(r"[\r\n\x00]", requested_session_id)
+                or _is_path_unsafe(requested_session_id)
+            ):
+                return web.json_response(
+                    _openai_error("Invalid session ID", code="invalid_session_id"),
+                    status=400,
+                )
+            if len(requested_session_id) > self._MAX_SESSION_HEADER_LEN:
+                return web.json_response(
+                    _openai_error("Session ID too long", code="invalid_session_id"),
+                    status=400,
+                )
+        session_id = requested_session_id or stored_session_id or run_id
         # Approval queues gate host-side tool execution and must be isolated
         # per API run.  Client-provided session IDs and memory session keys are
         # conversation/memory scopes, not authorization namespaces: multiple
         # concurrent runs can intentionally share them, and resolving an
         # approval for one run must not unblock another run's dangerous command.
         approval_session_key = run_id
+        session_cwd = self._workspace_cwd_from_body(body)
         ephemeral_system_prompt = instructions
         loop = asyncio.get_running_loop()
         q: "asyncio.Queue[Optional[Dict]]" = asyncio.Queue()
@@ -4288,14 +4356,28 @@ class APIServerAdapter(BasePlatformAdapter):
         async def _run_and_close():
             try:
                 self._set_run_status(run_id, "running")
-                agent = self._create_agent(
-                    ephemeral_system_prompt=ephemeral_system_prompt,
+                from gateway.session_context import clear_session_vars
+
+                create_session_tokens = self._bind_api_server_session(
+                    chat_id=session_id,
+                    session_key=gateway_session_key or session_id,
                     session_id=session_id,
-                    stream_delta_callback=_text_cb,
-                    tool_progress_callback=event_cb,
-                    gateway_session_key=gateway_session_key,
-                    route=route,
+                    cwd=session_cwd,
                 )
+                try:
+                    agent = self._create_agent(
+                        ephemeral_system_prompt=ephemeral_system_prompt,
+                        session_id=session_id,
+                        stream_delta_callback=_text_cb,
+                        tool_progress_callback=event_cb,
+                        gateway_session_key=gateway_session_key,
+                        route=route,
+                    )
+                finally:
+                    try:
+                        clear_session_vars(create_session_tokens)
+                    except Exception:
+                        pass
                 self._active_run_agents[run_id] = agent
 
                 def _approval_notify(approval_data: Dict[str, Any]) -> None:
@@ -4342,7 +4424,10 @@ class APIServerAdapter(BasePlatformAdapter):
                         # environment state.
                         approval_token = set_current_session_key(approval_session_key)
                         session_tokens = self._bind_api_server_session(
-                            session_key=approval_session_key,
+                            chat_id=session_id,
+                            session_key=gateway_session_key or session_id,
+                            session_id=session_id,
+                            cwd=session_cwd,
                         )
                         register_gateway_notify(approval_session_key, _approval_notify)
                         r = agent.run_conversation(
@@ -4391,16 +4476,19 @@ class APIServerAdapter(BasePlatformAdapter):
                     )
                 else:
                     final_response = result.get("final_response", "") if isinstance(result, dict) else ""
+                    effective_session_id = result.get("session_id", session_id) if isinstance(result, dict) else session_id
                     q.put_nowait({
                         "event": "run.completed",
                         "run_id": run_id,
                         "timestamp": time.time(),
+                        "session_id": effective_session_id,
                         "output": final_response,
                         "usage": usage,
                     })
                     self._set_run_status(
                         run_id,
                         "completed",
+                        session_id=effective_session_id,
                         output=final_response,
                         usage=usage,
                         last_event="run.completed",
diff --git a/gateway/run.py b/gateway/run.py
index 54f38d1bb..5dc9452a6 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -2493,7 +2493,9 @@ def _parse_session_key(session_key: str) -> "dict | None":
     """Parse a session key into its component parts.
 
     Session keys follow the format
-    ``agent:main:{platform}:{chat_type}:{chat_id}[:{extra}...]``.
+    ``agent:{profile}:{platform}:{chat_type}:{chat_id}[:{extra}...]``.
+    The legacy/default namespace is ``agent:main``; named profiles occupy the
+    same second segment (for example ``agent:coder:telegram:dm:123``).
     Returns a dict with ``platform``, ``chat_type``, ``chat_id``, and
     optionally ``thread_id`` keys, or None if the key doesn't match.
 
@@ -2503,11 +2505,12 @@ def _parse_session_key(session_key: str) -> "dict | None":
     thread_id, so we leave ``thread_id`` out to avoid mis-routing.
     """
     parts = session_key.split(":")
-    if len(parts) >= 5 and parts[0] == "agent" and parts[1] == "main":
+    if len(parts) >= 5 and parts[0] == "agent" and parts[1]:
         result = {
             "platform": parts[2],
             "chat_type": parts[3],
             "chat_id": parts[4],
+            "profile": parts[1],
         }
         if len(parts) > 5 and parts[3] in {"dm", "thread"}:
             result["thread_id"] = parts[5]
@@ -15284,9 +15287,16 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
         Falling back to the currently active foreground event is what causes
         cross-topic bleed, so don't do that.
         """
-        from gateway.session import SessionSource
+        from gateway.session import SessionSource, normalize_enterprise_agent_scope_key
-
+
         session_key = str(evt.get("session_key") or "").strip()
+        def _with_enterprise_scope(source):
+            canonical = normalize_enterprise_agent_scope_key(session_key)
+            if canonical:
+                source.enterprise_session_key = canonical
+                source.delivered_via_upstream_relay = True
+            return source
+
         derived_platform = ""
         derived_chat_type = ""
         derived_chat_id = ""
@@ -15296,7 +15306,7 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
                 self.session_store._ensure_loaded()
                 entry = self.session_store._entries.get(session_key)
                 if entry and getattr(entry, "origin", None):
-                    return entry.origin
+                    return _with_enterprise_scope(entry.origin)
             except Exception as exc:
                 logger.debug(
                     "Synthetic process-event session-store lookup failed for %s: %s",
@@ -15306,7 +15316,7 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
-
+
             cached_source = self._get_cached_session_source(session_key)
             if cached_source is not None:
-                return cached_source
+                return _with_enterprise_scope(cached_source)
-
+
             _parsed = _parse_session_key(session_key)
             if _parsed:
@@ -15349,10 +15359,15 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
         return SessionSource(
             platform=platform,
             chat_id=chat_id,
             chat_type=chat_type,
             thread_id=str(evt.get("thread_id") or "").strip() or None,
             user_id=str(evt.get("user_id") or "").strip() or None,
+            user_id_alt=str(evt.get("user_id_alt") or "").strip() or None,
             user_name=str(evt.get("user_name") or "").strip() or None,
+            message_id=str(evt.get("message_id") or "").strip() or None,
+            profile=str(evt.get("profile") or "").strip() or None,
+            enterprise_session_key=normalize_enterprise_agent_scope_key(session_key),
+            delivered_via_upstream_relay=bool(normalize_enterprise_agent_scope_key(session_key)),
         )
 
     async def _inject_watch_notification(self, synth_text: str, evt: dict) -> None:
@@ -15360,15 +15375,13 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
         expects. Best-effort: a CLI-origin event (empty session_key) is left
         as-is and simply won't route on the gateway.
         """
-        if evt.get("platform"):
-            return  # already enriched
         parsed = _parse_session_key(evt.get("session_key", "") or "")
         if not parsed:
             return
-        evt["platform"] = parsed.get("platform", "")
-        evt["chat_type"] = parsed.get("chat_type", "")
-        evt["chat_id"] = parsed.get("chat_id", "")
-        if parsed.get("thread_id"):
+        for key in ("platform", "chat_type", "chat_id", "profile"):
+            if not evt.get(key):
+                evt[key] = parsed.get(key, "")
+        if parsed.get("thread_id") and not evt.get("thread_id"):
             evt["thread_id"] = parsed["thread_id"]
 
     async def _async_delegation_watcher(self, interval: float = 2.0) -> None:
diff --git a/hermes_state.py b/hermes_state.py
index a0b6dfd09..3b6b569a2 100644
--- a/hermes_state.py
+++ b/hermes_state.py
@@ -2820,7 +2820,11 @@ class SessionDB:
             row = cursor.fetchone()
         return dict(row) if row else None
 
-    def resolve_session_by_title(self, title: str) -> Optional[str]:
+    def resolve_session_by_title(
+        self,
+        title: str,
+        session_key: str = None,
+    ) -> Optional[str]:
         """Resolve a title to a session ID, preferring the latest in a lineage.
 
         If the exact title exists, returns that session's ID.
@@ -2828,18 +2832,38 @@ class SessionDB:
         If the exact title exists AND numbered variants exist, returns the
         latest numbered variant (the most recent continuation).
         """
-        # First try exact match
-        exact = self.get_session_by_title(title)
+        clean_session_key = str(session_key or "").strip()
+
+        # First try exact match. Scoped agents resolve inside their own
+        # durable session_key so a duplicate title in another agent cannot
+        # win the lookup before the caller-side visibility check.
+        if clean_session_key:
+            with self._lock:
+                exact_row = self._conn.execute(
+                    "SELECT * FROM sessions WHERE title = ? AND session_key = ?",
+                    (title, clean_session_key),
+                ).fetchone()
+            exact = dict(exact_row) if exact_row else None
+        else:
+            exact = self.get_session_by_title(title)
 
         # Also search for numbered variants: "title #2", "title #3", etc.
         # Escape SQL LIKE wildcards (%, _) in the title to prevent false matches
         escaped = title.replace("\\", "\\\\").replace("%", "\\%").replace("_", "\\_")
         with self._lock:
-            cursor = self._conn.execute(
-                "SELECT id, title, started_at FROM sessions "
-                "WHERE title LIKE ? ESCAPE '\\' ORDER BY started_at DESC",
-                (f"{escaped} #%",),
-            )
+            if clean_session_key:
+                cursor = self._conn.execute(
+                    "SELECT id, title, started_at FROM sessions "
+                    "WHERE title LIKE ? ESCAPE '\\' AND session_key = ? "
+                    "ORDER BY started_at DESC",
+                    (f"{escaped} #%", clean_session_key),
+                )
+            else:
+                cursor = self._conn.execute(
+                    "SELECT id, title, started_at FROM sessions "
+                    "WHERE title LIKE ? ESCAPE '\\' ORDER BY started_at DESC",
+                    (f"{escaped} #%",),
+                )
             numbered = cursor.fetchall()
 
         if numbered:
@@ -3011,6 +3035,7 @@ class SessionDB:
         id_query: str = None,
         search_query: str = None,
         compact_rows: bool = False,
+        session_key: str = None,
     ) -> List[Dict[str, Any]]:
         """List sessions with preview (first user message) and last active timestamp.
 
@@ -3083,6 +3108,9 @@ class SessionDB:
             clause, clause_params = _cwd_prefix_clause(cwd_prefix)
             where_clauses.append(clause)
             params.extend(clause_params)
+        if session_key:
+            where_clauses.append("s.session_key = ?")
+            params.append(str(session_key))
         if min_message_count > 0:
             where_clauses.append("s.message_count >= ?")
             params.append(min_message_count)
@@ -4519,6 +4547,7 @@ class SessionDB:
         offset: int = 0,
         sort: str = None,
         include_inactive: bool = False,
+        session_key: str = None,
     ) -> List[Dict[str, Any]]:
         """
         Full-text search across session messages using FTS5.
@@ -4596,6 +4625,10 @@ class SessionDB:
             where_clauses.append(f"s.source NOT IN ({exclude_placeholders})")
             params.extend(exclude_sources)
 
+        if session_key:
+            where_clauses.append("s.session_key = ?")
+            params.append(str(session_key))
+
         if role_filter:
             role_placeholders = ",".join("?" for _ in role_filter)
             where_clauses.append(f"m.role IN ({role_placeholders})")
@@ -4673,6 +4706,9 @@ class SessionDB:
                 if exclude_sources is not None:
                     tri_where.append(f"s.source NOT IN ({','.join('?' for _ in exclude_sources)})")
                     tri_params.extend(exclude_sources)
+                if session_key:
+                    tri_where.append("s.session_key = ?")
+                    tri_params.append(str(session_key))
                 if role_filter:
                     tri_where.append(f"m.role IN ({','.join('?' for _ in role_filter)})")
                     tri_params.extend(role_filter)
@@ -4730,6 +4766,9 @@ class SessionDB:
                 if exclude_sources is not None:
                     like_where.append(f"s.source NOT IN ({','.join('?' for _ in exclude_sources)})")
                     like_params.extend(exclude_sources)
+                if session_key:
+                    like_where.append("s.session_key = ?")
+                    like_params.append(str(session_key))
                 if role_filter:
                     like_where.append(f"m.role IN ({','.join('?' for _ in role_filter)})")
                     like_params.extend(role_filter)
diff --git a/model_tools.py b/model_tools.py
index c59c189e3..d365bb55c 100644
--- a/model_tools.py
+++ b/model_tools.py
@@ -1037,6 +1037,7 @@ def handle_function_call(
     tool_request_middleware_trace: Optional[List[Dict[str, Any]]] = None,
     enabled_toolsets: Optional[List[str]] = None,
     disabled_toolsets: Optional[List[str]] = None,
+    agent_scope_key: Optional[str] = None,
 ) -> str:
     """
     Main function call dispatcher that routes calls to the tool registry.
@@ -1141,6 +1142,7 @@ def handle_function_call(
                 tool_request_middleware_trace=list(_tool_middleware_trace),
                 enabled_toolsets=enabled_toolsets,
                 disabled_toolsets=disabled_toolsets,
+                agent_scope_key=agent_scope_key,
             )
 
     _tool_original_args = dict(function_args)
@@ -1270,11 +1272,16 @@ def handle_function_call(
                     )
             else:
                 def _dispatch(next_args: Dict[str, Any]) -> Any:
+                    dispatch_kwargs: Dict[str, Any] = {
+                        "task_id": task_id,
+                        "session_id": session_id,
+                        "user_task": user_task,
+                    }
+                    if function_name == "terminal" and agent_scope_key:
+                        dispatch_kwargs["agent_scope_key"] = agent_scope_key
                     return registry.dispatch(
                         function_name, next_args,
-                        task_id=task_id,
-                        session_id=session_id,
-                        user_task=user_task,
+                        **dispatch_kwargs,
                     )
             from hermes_cli.middleware import run_tool_execution_middleware
 
diff --git a/plugins/memory/byterover/__init__.py b/plugins/memory/byterover/__init__.py
index 5161bacf4..b647c4e8a 100644
--- a/plugins/memory/byterover/__init__.py
+++ b/plugins/memory/byterover/__init__.py
@@ -25,6 +25,8 @@ from __future__ import annotations
 import json
 import logging
 import os
+import hashlib
+import re
 import shutil
 import subprocess
 import threading
@@ -162,10 +164,23 @@ def _run_brv(args: List[str], timeout: int = _QUERY_TIMEOUT,
         return {"success": False, "error": str(e)}
 
 
-def _get_brv_cwd() -> Path:
+def _scope_dir_name(scope_key: str) -> str:
+    raw = str(scope_key or "").strip()
+    digest = hashlib.sha256(raw.encode("utf-8")).hexdigest()[:12]
+    safe = re.sub(r"[^A-Za-z0-9_.-]+", "-", raw).strip(".-") or "agent"
+    if len(safe) > 80:
+        safe = safe[:80].rstrip(".-") or "agent"
+    return f"{safe}-{digest}"
+
+
+def _get_brv_cwd(scope_key: str = "") -> Path:
     """Profile-scoped working directory for the brv context tree."""
     from hermes_constants import get_hermes_home
-    return get_hermes_home() / "byterover"
+    base = get_hermes_home() / "byterover"
+    clean_scope = str(scope_key or "").strip()
+    if clean_scope:
+        return base / "agents" / _scope_dir_name(clean_scope)
+    return base
 
 
 # ---------------------------------------------------------------------------
@@ -254,7 +269,8 @@ class ByteRoverMemoryProvider(MemoryProvider):
         ]
 
     def initialize(self, session_id: str, **kwargs) -> None:
-        self._cwd = str(_get_brv_cwd())
+        scope_key = kwargs.get("agent_scope_key") or kwargs.get("gateway_session_key") or ""
+        self._cwd = str(_get_brv_cwd(str(scope_key or "")))
         self._session_id = session_id
         self._turn_count = 0
         Path(self._cwd).mkdir(parents=True, exist_ok=True)
diff --git a/plugins/memory/hindsight/__init__.py b/plugins/memory/hindsight/__init__.py
index 9f5974b7b..6456f1709 100644
--- a/plugins/memory/hindsight/__init__.py
+++ b/plugins/memory/hindsight/__init__.py
@@ -32,6 +32,7 @@ from __future__ import annotations
 
 import asyncio
 import atexit
+import hashlib
 import importlib
 import json
 import logging
@@ -614,6 +615,18 @@ def _resolve_bank_id_template(template: str, fallback: str, **placeholders: str)
     return rendered or fallback
 
 
+def _scoped_default_bank_id(scope_key: str) -> str:
+    """Return a collision-resistant default Hindsight bank id for an agent scope."""
+    clean = str(scope_key or "").strip()
+    if not clean:
+        return "hermes"
+    digest = hashlib.sha256(clean.encode("utf-8")).hexdigest()[:12]
+    segment = _sanitize_bank_segment(clean) or "agent"
+    if len(segment) > 80:
+        segment = segment[:80].strip("-_") or "agent"
+    return f"hermes-{segment}-{digest}"
+
+
 # ---------------------------------------------------------------------------
 # MemoryProvider implementation
 # ---------------------------------------------------------------------------
@@ -1286,6 +1299,9 @@ class HindsightMemoryProvider(MemoryProvider):
         banks = cfg_get(self._config, "banks", "hermes", default={})
         static_bank_id = self._config.get("bank_id") or banks.get("bankId", "hermes")
         self._bank_id_template = self._config.get("bank_id_template", "") or ""
+        scope_key = str(kwargs.get("agent_scope_key") or kwargs.get("gateway_session_key") or "").strip()
+        if scope_key and not self._bank_id_template and str(static_bank_id or "").strip() in {"", "hermes"}:
+            static_bank_id = _scoped_default_bank_id(scope_key)
         self._bank_id = _resolve_bank_id_template(
             self._bank_id_template,
             fallback=static_bank_id,
diff --git a/plugins/memory/holographic/__init__.py b/plugins/memory/holographic/__init__.py
index fa8dae976..cc712b493 100644
--- a/plugins/memory/holographic/__init__.py
+++ b/plugins/memory/holographic/__init__.py
@@ -19,6 +19,7 @@ from __future__ import annotations
 
 import json
 import logging
+import hashlib
 import re
 from typing import Any, Dict, List
 
@@ -31,6 +32,15 @@ from hermes_cli.config import cfg_get
 logger = logging.getLogger(__name__)
 
 
+def _scope_dir_name(scope_key: str) -> str:
+    raw = str(scope_key or "").strip()
+    digest = hashlib.sha256(raw.encode("utf-8")).hexdigest()[:12]
+    safe = re.sub(r"[^A-Za-z0-9_.-]+", "-", raw).strip(".-") or "agent"
+    if len(safe) > 80:
+        safe = safe[:80].rstrip(".-") or "agent"
+    return f"{safe}-{digest}"
+
+
 # ---------------------------------------------------------------------------
 # Tool schemas (unchanged from original PR)
 # ---------------------------------------------------------------------------
@@ -159,7 +169,20 @@ class HolographicMemoryProvider(MemoryProvider):
         from hermes_constants import get_hermes_home
         _hermes_home = str(get_hermes_home())
         _default_db = _hermes_home + "/memory_store.db"
-        db_path = self._config.get("db_path", _default_db)
+        scope_key = str(kwargs.get("agent_scope_key") or kwargs.get("gateway_session_key") or "").strip()
+        configured_db_path = self._config.get("db_path")
+        configured_text = str(configured_db_path or "").strip()
+        default_config_values = {
+            "",
+            _default_db,
+            "$HERMES_HOME/memory_store.db",
+            "${HERMES_HOME}/memory_store.db",
+        }
+        db_path = (
+            f"{_hermes_home}/memory_providers/holographic/agents/{_scope_dir_name(scope_key)}/memory_store.db"
+            if scope_key and configured_text in default_config_values
+            else (configured_db_path or _default_db)
+        )
         # Expand $HERMES_HOME in user-supplied paths so config values like
         # "$HERMES_HOME/memory_store.db" or "~/.hermes/memory_store.db" both
         # resolve to the active profile's directory.
diff --git a/plugins/memory/mem0/__init__.py b/plugins/memory/mem0/__init__.py
index 35f413ed3..8d7a8d37a 100644
--- a/plugins/memory/mem0/__init__.py
+++ b/plugins/memory/mem0/__init__.py
@@ -338,6 +338,7 @@ class Mem0MemoryProvider(MemoryProvider):
         self._mode = self._config.get("mode", "platform")
         self._api_key = self._config.get("api_key", "")
         self._host = self._config.get("host", "")
+        scope_key = kwargs.get("agent_scope_key") or kwargs.get("gateway_session_key")
         # Resolution order for user_id:
         #   1. Operator-configured MEM0_USER_ID (env or $HERMES_HOME/mem0.json) —
         #      the canonical principal, applied across every gateway so the same
@@ -352,8 +353,8 @@ class Mem0MemoryProvider(MemoryProvider):
         configured = self._config.get("user_id")
         if configured == _DEFAULT_USER_ID:
             configured = None
-        self._user_id = configured or kwargs.get("user_id") or _DEFAULT_USER_ID
-        self._agent_id = self._config.get("agent_id", "hermes")
+        self._user_id = configured or kwargs.get("user_id") or scope_key or _DEFAULT_USER_ID
+        self._agent_id = kwargs.get("agent_id") or scope_key or self._config.get("agent_id", "hermes")
         # Persisted rerank preference (setup wizard / mem0.json). Used as the
         # DEFAULT for mem0_search when the model doesn't pass ``rerank``
         # explicitly; per-call args still win. Platform-only feature — other
diff --git a/plugins/memory/openviking/__init__.py b/plugins/memory/openviking/__init__.py
index 5aef3b908..a3a711303 100644
--- a/plugins/memory/openviking/__init__.py
+++ b/plugins/memory/openviking/__init__.py
@@ -2132,11 +2132,15 @@ class OpenVikingMemoryProvider(MemoryProvider):
 
     def initialize(self, session_id: str, **kwargs) -> None:
         settings = _resolve_connection_settings(_load_hermes_openviking_config())
+        scope_key = kwargs.get("agent_scope_key") or kwargs.get("gateway_session_key")
         self._endpoint = settings["endpoint"]
         self._api_key = settings["api_key"]
         self._account = settings["account"]
-        self._user = settings["user"]
-        self._agent = settings["agent"]
+        configured_agent = settings["agent"]
+        if configured_agent == _DEFAULT_AGENT and (kwargs.get("agent_id") or scope_key):
+            configured_agent = ""
+        self._user = settings["user"] or kwargs.get("user_id") or scope_key or "default"
+        self._agent = configured_agent or kwargs.get("agent_id") or scope_key or _DEFAULT_AGENT
         self._session_id = session_id
         self._turn_count = 0
         warning_callback = (
diff --git a/plugins/memory/supermemory/__init__.py b/plugins/memory/supermemory/__init__.py
index 1d086f47d..635ea5b0c 100644
--- a/plugins/memory/supermemory/__init__.py
+++ b/plugins/memory/supermemory/__init__.py
@@ -6,6 +6,7 @@ explicit memory tools, cleaned turn capture, and session-end conversation ingest
 
 from __future__ import annotations
 
+import hashlib
 import json
 import logging
 import os
@@ -77,6 +78,18 @@ def _sanitize_tag(raw: str) -> str:
     return tag.strip("_") or _DEFAULT_CONTAINER_TAG
 
 
+def _scoped_default_container_tag(scope_key: str) -> str:
+    """Derive a safe, collision-resistant default container for one agent."""
+    clean = str(scope_key or "").strip()
+    if not clean:
+        return _DEFAULT_CONTAINER_TAG
+    digest = hashlib.sha256(clean.encode("utf-8")).hexdigest()[:12]
+    slug = _sanitize_tag(clean)
+    if len(slug) > 48:
+        slug = slug[:48].rstrip("_") or "agent"
+    return f"{_DEFAULT_CONTAINER_TAG}_{slug}_{digest}"
+
+
 def _clamp_entity_context(text: str) -> str:
     if not text:
         return _DEFAULT_ENTITY_CONTEXT
@@ -635,7 +648,11 @@ class SupermemoryMemoryProvider(MemoryProvider):
         env_tag = os.environ.get("SUPERMEMORY_CONTAINER_TAG", "").strip()
         raw_tag = env_tag or self._config["container_tag"]
         identity = kwargs.get("agent_identity", "default")
-        self._container_tag = _sanitize_tag(raw_tag.replace("{identity}", identity))
+        scope_key = kwargs.get("agent_scope_key") or kwargs.get("gateway_session_key")
+        if scope_key and raw_tag == _DEFAULT_CONTAINER_TAG:
+            self._container_tag = _scoped_default_container_tag(scope_key)
+        else:
+            self._container_tag = _sanitize_tag(raw_tag.replace("{identity}", identity))
 
         self._auto_recall = self._config["auto_recall"]
         self._auto_capture = self._config["auto_capture"]
diff --git a/run_agent.py b/run_agent.py
index 61e8657b9..6655e07bc 100644
--- a/run_agent.py
+++ b/run_agent.py
@@ -594,6 +594,13 @@ class AIAgent:
         if self._session_db_created or not self._session_db:
             return
         source = _session_source_for_agent(self.platform)
+        try:
+            from agent.runtime_cwd import resolve_context_cwd
+
+            context_cwd = resolve_context_cwd()
+            session_cwd = str(context_cwd) if context_cwd else _launch_cwd_for_session(source)
+        except Exception:
+            session_cwd = _launch_cwd_for_session(source)
         try:
             self._session_db.create_session(
                 session_id=self.session_id,
@@ -602,8 +609,9 @@ class AIAgent:
                 model_config=self._session_init_model_config,
                 system_prompt=self._cached_system_prompt,
                 user_id=None,
+                session_key=getattr(self, "_gateway_session_key", None),
                 parent_session_id=self._parent_session_id,
-                cwd=_launch_cwd_for_session(source),
+                cwd=session_cwd,
             )
             self._session_db_created = True
         except Exception as e:
@@ -3474,19 +3482,32 @@ class AIAgent:
         independently guarded so a failure in one does not prevent the rest.
         """
         task_id = getattr(self, "session_id", None) or ""
+        terminal_task_id = task_id
+        gateway_session_key = getattr(self, "_gateway_session_key", None)
+        if gateway_session_key:
+            try:
+                from tools.terminal_tool import _resolve_container_task_id
 
-        # 1. Kill background processes for this task
-        try:
-            from tools.process_registry import process_registry
-            process_registry.kill_all(task_id=task_id)
-        except Exception:
-            pass
+                terminal_task_id = _resolve_container_task_id(
+                    task_id,
+                    gateway_session_key,
+                )
+            except Exception:
+                pass
 
-        # 2. Clean terminal sandbox environments
-        try:
-            cleanup_vm(task_id)
-        except Exception:
-            pass
+        if not getattr(self, "_shares_parent_terminal_scope", False):
+            # 1. Kill background processes for this task
+            try:
+                from tools.process_registry import process_registry
+                process_registry.kill_all(task_id=terminal_task_id)
+            except Exception:
+                pass
+
+            # 2. Clean terminal sandbox environments
+            try:
+                cleanup_vm(terminal_task_id)
+            except Exception:
+                pass
 
         # 3. Clean browser daemon sessions
         try:
diff --git a/tools/async_delegation.py b/tools/async_delegation.py
index 9b4b0e964..c8b1ff7be 100644
--- a/tools/async_delegation.py
+++ b/tools/async_delegation.py
@@ -130,6 +130,7 @@ def dispatch_async_delegation(
     model: Optional[str],
     session_key: str,
     parent_session_id: Optional[str] = None,
+    routing_metadata: Optional[Dict[str, Any]] = None,
     runner: Callable[[], Dict[str, Any]],
     origin_ui_session_id: str = "",
     interrupt_fn: Optional[Callable[[], None]] = None,
@@ -181,6 +182,7 @@ def dispatch_async_delegation(
         "session_key": session_key,
         "origin_ui_session_id": origin_ui_session_id,
         "parent_session_id": parent_session_id,
+        "routing_metadata": dict(routing_metadata or {}),
         "status": "running",
         "dispatched_at": dispatched_at,
         "completed_at": None,
@@ -309,6 +311,22 @@ def _push_completion_event(
         "completed_at": completed_at,
         "exit_reason": result.get("exit_reason"),
     }
+    routing_metadata = record.get("routing_metadata")
+    if isinstance(routing_metadata, dict):
+        for key in (
+            "platform",
+            "chat_type",
+            "chat_id",
+            "thread_id",
+            "user_id",
+            "user_id_alt",
+            "user_name",
+            "message_id",
+            "profile",
+        ):
+            value = routing_metadata.get(key)
+            if value:
+                evt[key] = value
     try:
         process_registry.completion_queue.put(evt)
     except Exception as exc:  # pragma: no cover
@@ -328,6 +346,7 @@ def dispatch_async_delegation_batch(
     model: Optional[str],
     session_key: str,
     parent_session_id: Optional[str] = None,
+    routing_metadata: Optional[Dict[str, Any]] = None,
     runner: Callable[[], Dict[str, Any]],
     origin_ui_session_id: str = "",
     interrupt_fn: Optional[Callable[[], None]] = None,
@@ -371,6 +390,7 @@ def dispatch_async_delegation_batch(
         "session_key": session_key,
         "origin_ui_session_id": origin_ui_session_id,
         "parent_session_id": parent_session_id,
+        "routing_metadata": dict(routing_metadata or {}),
         "status": "running",
         "dispatched_at": dispatched_at,
         "completed_at": None,
@@ -486,6 +506,22 @@ def _finalize_batch(
         "dispatched_at": dispatched_at,
         "completed_at": completed_at,
     }
+    routing_metadata = event_record.get("routing_metadata")
+    if isinstance(routing_metadata, dict):
+        for key in (
+            "platform",
+            "chat_type",
+            "chat_id",
+            "thread_id",
+            "user_id",
+            "user_id_alt",
+            "user_name",
+            "message_id",
+            "profile",
+        ):
+            value = routing_metadata.get(key)
+            if value:
+                evt[key] = value
     try:
         process_registry.completion_queue.put(evt)
     except Exception as exc:  # pragma: no cover
diff --git a/tools/code_execution_tool.py b/tools/code_execution_tool.py
index 54772d7d1..28008507c 100644
--- a/tools/code_execution_tool.py
+++ b/tools/code_execution_tool.py
@@ -1756,11 +1756,14 @@ def _resolve_child_cwd(mode: str, staging_dir: str) -> str:
     """
     if mode != "project":
         return staging_dir
-    raw = os.environ.get("TERMINAL_CWD", "").strip()
-    if raw:
-        expanded = os.path.expanduser(raw)
-        if os.path.isdir(expanded):
-            return expanded
+    try:
+        from agent.runtime_cwd import resolve_agent_cwd
+
+        resolved = resolve_agent_cwd()
+        if resolved.is_dir():
+            return str(resolved)
+    except Exception:
+        pass
     here = os.getcwd()
     if os.path.isdir(here):
         return here
diff --git a/tools/delegate_tool.py b/tools/delegate_tool.py
index 911e25204..1a23c2adf 100644
--- a/tools/delegate_tool.py
+++ b/tools/delegate_tool.py
@@ -743,7 +743,14 @@ def _resolve_workspace_hint(parent_agent) -> Optional[str]:
     teaching subagents a fake container path while still helping them avoid
     guessing `/workspace/...` for local repo tasks.
     """
+    try:
+        from agent.runtime_cwd import resolve_context_cwd
+
+        context_cwd = resolve_context_cwd()
+    except Exception:
+        context_cwd = None
     candidates = [
+        str(context_cwd) if context_cwd else None,
         os.getenv("TERMINAL_CWD"),
         getattr(
             getattr(parent_agent, "_subdirectory_hints", None), "working_dir", None
@@ -1316,6 +1323,7 @@ def _build_child_agent(
         ephemeral_system_prompt=child_prompt,
         log_prefix=f"[subagent-{task_index}]",
         platform="subagent",
+        gateway_session_key=getattr(parent_agent, "_gateway_session_key", None),
         skip_context_files=True,
         skip_memory=True,
         clarify_callback=None,
@@ -1330,6 +1338,13 @@ def _build_child_agent(
         tool_progress_callback=child_progress_cb,
         iteration_budget=None,  # fresh budget per subagent
     )
+    # Gateway delegates share the parent's stable terminal scope. Their
+    # teardown must not destroy that shared sandbox or kill the parent's
+    # background jobs. CLI/RL children without such a scope retain their
+    # ordinary independent cleanup contract.
+    child._shares_parent_terminal_scope = bool(
+        str(getattr(parent_agent, "_gateway_session_key", "") or "").strip()
+    )
     child._print_fn = getattr(parent_agent, "_print_fn", None)
     # Now the child exists, its session id can ride on every relayed event
     # (including the spawn_requested below — first emit happens after this).
@@ -1924,7 +1939,11 @@ def _run_single_child(
                 stream_callback=_relay_child_text,
             )
 
-        _child_future = _timeout_executor.submit(_run_with_thread_capture)
+        from tools.thread_context import propagate_context_to_thread
+
+        _child_future = _timeout_executor.submit(
+            propagate_context_to_thread(_run_with_thread_capture)
+        )
         try:
             result = _child_future.result(timeout=child_timeout)
         except Exception as _timeout_exc:
@@ -2339,6 +2358,67 @@ def _recover_tasks_from_json_string(
     return parsed, None
 
 
+def _async_delegation_routing_metadata(
+    parent_agent: Any,
+    session_key: str,
+) -> Dict[str, Any]:
+    """Capture gateway routing fields for detached delegation completions.
+
+    Async subagents finish on daemon worker threads after the parent turn has
+    returned.  The gateway can often reconstruct the route from session_key, but
+    profile-scoped keys and threaded/group channels need the original source
+    metadata for precise delivery.
+    """
+    metadata: Dict[str, Any] = {}
+    if session_key:
+        metadata["session_key"] = session_key
+
+    def _metadata_text(value: Any) -> Optional[str]:
+        if value is None:
+            return None
+        enum_value = getattr(value, "value", None)
+        if isinstance(enum_value, str):
+            value = enum_value
+        if not isinstance(value, (str, int, float)):
+            return None
+        text = str(value).strip()
+        return text or None
+
+    for attr, key in (
+        ("platform", "platform"),
+        ("_chat_type", "chat_type"),
+        ("_chat_id", "chat_id"),
+        ("_thread_id", "thread_id"),
+        ("_user_id", "user_id"),
+        ("_user_id_alt", "user_id_alt"),
+        ("_user_name", "user_name"),
+    ):
+        try:
+            value = getattr(parent_agent, attr, None)
+        except Exception:
+            value = None
+        text = _metadata_text(value)
+        if text:
+            metadata[key] = text
+
+    try:
+        from gateway.session_context import get_session_env
+
+        for env_name, key in (
+            ("HERMES_SESSION_MESSAGE_ID", "message_id"),
+            ("HERMES_SESSION_PROFILE", "profile"),
+        ):
+            if key in metadata:
+                continue
+            text = _metadata_text(get_session_env(env_name, ""))
+            if text:
+                metadata[key] = text
+    except Exception:
+        pass
+
+    return metadata
+
+
 def delegate_task(
     goal: Optional[str] = None,
     context: Optional[str] = None,
@@ -2535,11 +2615,13 @@ def delegate_task(
             # normally, but if the parent is interrupted while a child is
             # wedged, the abandoned worker must not block interpreter exit.
             from tools.daemon_pool import DaemonThreadPoolExecutor
+            from tools.thread_context import propagate_context_to_thread
+
             with DaemonThreadPoolExecutor(max_workers=max_children) as executor:
                 futures = {}
                 for i, t, child in children:
                     future = executor.submit(
-                        _run_single_child,
+                        propagate_context_to_thread(_run_single_child),
                         task_index=i,
                         goal=t["goal"],
                         child=child,
@@ -2795,7 +2877,10 @@ def delegate_task(
                 )
             return json.dumps(_sync_result, ensure_ascii=False)
 
-        _session_key = get_current_session_key(default="")
+        _session_key = (
+            str(getattr(parent_agent, "_gateway_session_key", "") or "")
+            or get_current_session_key(default="")
+        )
         _origin_ui_session_id = ""
         try:
             from gateway.session_context import get_session_env
@@ -2816,6 +2901,10 @@ def delegate_task(
         except Exception:
             _origin_ui_session_id = ""
         _parent_session_id = getattr(parent_agent, "session_id", None)
+        _routing_metadata = _async_delegation_routing_metadata(
+            parent_agent,
+            _session_key,
+        )
         _child_agents = [c for (_, _, c) in children]
 
         # Detach every child from the parent's interrupt-propagation list — the
@@ -2858,6 +2947,7 @@ def delegate_task(
             session_key=_session_key,
             origin_ui_session_id=_origin_ui_session_id,
             parent_session_id=_parent_session_id,
+            routing_metadata=_routing_metadata,
             runner=_batch_runner,
             interrupt_fn=_batch_interrupt,
             max_async_children=_get_max_async_children(),
diff --git a/tools/file_tools.py b/tools/file_tools.py
index 61e6ecc58..9e95a77cd 100644
--- a/tools/file_tools.py
+++ b/tools/file_tools.py
@@ -401,7 +401,12 @@ def _resolve_base_dir(
     if root:
         base_text = _expand_tilde(root)
     else:
-        base_text = os.getcwd()
+        try:
+            from agent.runtime_cwd import resolve_agent_cwd
+
+            base_text = str(resolve_agent_cwd())
+        except Exception:
+            base_text = os.getcwd()
     if container_paths:
         if not posixpath.isabs(base_text):
             base_text = posixpath.join(os.getcwd(), base_text)
diff --git a/tools/memory_tool.py b/tools/memory_tool.py
index 08eeaa470..36087cfe7 100644
--- a/tools/memory_tool.py
+++ b/tools/memory_tool.py
@@ -26,6 +26,8 @@ Design:
 import json
 import logging
 import os
+import hashlib
+import re
 import tempfile
 import time
 from contextlib import contextmanager
@@ -52,9 +54,28 @@ logger = logging.getLogger(__name__)
 # (HERMES_HOME env var changes) are always respected.  The old module-level
 # constant was cached at import time and could go stale if a profile switch
 # happened after the first import.
-def get_memory_dir() -> Path:
-    """Return the profile-scoped memories directory."""
-    return get_hermes_home() / "memories"
+def _memory_scope_dir_name(scope_key: str) -> str:
+    """Return a filesystem-safe, stable directory name for an agent memory scope."""
+    raw = str(scope_key or "").strip()
+    digest = hashlib.sha256(raw.encode("utf-8")).hexdigest()[:12]
+    safe = re.sub(r"[^A-Za-z0-9_.-]+", "-", raw).strip(".-") or "agent"
+    if len(safe) > 80:
+        safe = safe[:80].rstrip(".-") or "agent"
+    return f"{safe}-{digest}"
+
+
+def get_memory_dir(scope_key: str | None = None) -> Path:
+    """Return the profile-scoped memories directory.
+
+    Without a scope key this preserves the historical profile-level memory
+    files. Gateway/API agents pass a stable session key so each agent gets its
+    own MEMORY.md and USER.md under ``memories/agents/``.
+    """
+    base = get_hermes_home() / "memories"
+    clean_scope = str(scope_key or "").strip()
+    if clean_scope:
+        return base / "agents" / _memory_scope_dir_name(clean_scope)
+    return base
 
 ENTRY_DELIMITER = "\n§\n"
 
@@ -127,11 +148,17 @@ class MemoryStore:
     # turn to budget exhaustion and suppress the user's reply (issue #42405).
     _MAX_CONSOLIDATION_FAILURES_PER_TURN = 3
 
-    def __init__(self, memory_char_limit: int = 2200, user_char_limit: int = 1375):
+    def __init__(
+        self,
+        memory_char_limit: int = 2200,
+        user_char_limit: int = 1375,
+        memory_scope_key: str = None,
+    ):
         self.memory_entries: List[str] = []
         self.user_entries: List[str] = []
         self.memory_char_limit = memory_char_limit
         self.user_char_limit = user_char_limit
+        self.memory_scope_key = str(memory_scope_key or "").strip()
         # Frozen snapshot for system prompt -- set once at load_from_disk()
         self._system_prompt_snapshot: Dict[str, str] = {"memory": "", "user": ""}
         # Per-turn counter of failed at-capacity consolidation attempts; reset
@@ -165,6 +192,17 @@ class MemoryStore:
             ),
         }
 
+    @property
+    def memory_dir(self) -> Path:
+        """Directory backing this store's MEMORY.md and USER.md files."""
+        try:
+            return get_memory_dir(self.memory_scope_key or None)
+        except TypeError:
+            # Test suites and third-party harnesses sometimes monkeypatch the
+            # old no-argument helper. Keep those harnesses compatible; real
+            # runtime code uses the scoped signature above.
+            return get_memory_dir()
+
     def load_from_disk(self):
         """Load entries from MEMORY.md and USER.md, capture system prompt snapshot.
 
@@ -182,7 +220,7 @@ class MemoryStore:
         Scanning is deterministic from disk bytes, so the snapshot remains
         stable for the entire session (prefix-cache invariant holds).
         """
-        mem_dir = get_memory_dir()
+        mem_dir = self.memory_dir
         mem_dir.mkdir(parents=True, exist_ok=True)
 
         self.memory_entries = self._read_file(mem_dir / "MEMORY.md")
@@ -277,9 +315,8 @@ class MemoryStore:
                     pass
             fd.close()
 
-    @staticmethod
-    def _path_for(target: str) -> Path:
-        mem_dir = get_memory_dir()
+    def _path_for(self, target: str) -> Path:
+        mem_dir = self.memory_dir
         if target == "user":
             return mem_dir / "USER.md"
         return mem_dir / "MEMORY.md"
@@ -308,7 +345,7 @@ class MemoryStore:
 
     def save_to_disk(self, target: str):
         """Persist entries to the appropriate file. Called after every mutation."""
-        get_memory_dir().mkdir(parents=True, exist_ok=True)
+        self.memory_dir.mkdir(parents=True, exist_ok=True)
         self._write_file(self._path_for(target), self._entries_for(target))
 
     def _entries_for(self, target: str) -> List[str]:
@@ -1148,5 +1185,3 @@ registry.register(
 )
 
 
-
-
diff --git a/tools/session_search_tool.py b/tools/session_search_tool.py
index d4d168ec3..0d7c702d4 100644
--- a/tools/session_search_tool.py
+++ b/tools/session_search_tool.py
@@ -120,6 +120,24 @@ def _order_for_recall(raw_results: List[Dict[str, Any]]) -> List[Dict[str, Any]]
     )
 
 
+def _normalize_session_key(value: Any) -> Optional[str]:
+    clean = str(value or "").strip()
+    return clean or None
+
+
+def _session_visible_for_key(db, session_id: str, current_session_key: str = None) -> bool:
+    """Return True when a session belongs to the caller's scoped agent key."""
+    current_session_key = _normalize_session_key(current_session_key)
+    if not current_session_key:
+        return True
+    try:
+        meta = db.get_session(session_id) or {}
+    except Exception:
+        logging.debug("session_key visibility lookup failed for %s", session_id, exc_info=True)
+        return False
+    return str(meta.get("session_key") or "").strip() == current_session_key
+
+
 def _shape_message(m: Dict[str, Any], anchor_id: Optional[int] = None) -> Dict[str, Any]:
     """Slim a message row for the tool response. Keeps content even if empty."""
     entry = {
@@ -208,7 +226,13 @@ def _locate_session_db(session_id: str):
     return None, None
 
 
-def _read_session(db, session_id: str, head: int = 20, tail: int = 10) -> str:
+def _read_session(
+    db,
+    session_id: str,
+    head: int = 20,
+    tail: int = 10,
+    current_session_key: str = None,
+) -> str:
     """Read shape: dump a whole session by id (head + tail when large).
 
     Serves the linked-session case — the user dropped an @session reference and
@@ -223,6 +247,9 @@ def _read_session(db, session_id: str, head: int = 20, tail: int = 10) -> str:
         meta = {}
     if not meta:
         return tool_error(f"session_id not found: {session_id}", success=False)
+    current_session_key = _normalize_session_key(current_session_key)
+    if current_session_key and str(meta.get("session_key") or "").strip() != current_session_key:
+        return tool_error(f"session_id not found: {session_id}", success=False)
 
     try:
         rows = db.get_messages(session_id)
@@ -257,13 +284,19 @@ def _read_session(db, session_id: str, head: int = 20, tail: int = 10) -> str:
     return json.dumps(response, ensure_ascii=False)
 
 
-def _list_recent_sessions(db, limit: int, current_session_id: str = None) -> str:
+def _list_recent_sessions(
+    db,
+    limit: int,
+    current_session_id: str = None,
+    current_session_key: str = None,
+) -> str:
     """Return metadata for the most recent sessions (no LLM calls, no FTS5)."""
     try:
         sessions = db.list_sessions_rich(
             limit=limit + 5,
             exclude_sources=list(_HIDDEN_SESSION_SOURCES),
             order_by_last_active=True,
+            session_key=_normalize_session_key(current_session_key),
         )  # fetch extra so we can skip current
 
         current_root = _resolve_to_parent(db, current_session_id) if current_session_id else None
@@ -306,6 +339,7 @@ def _scroll(
     around_message_id: int,
     window: int = 5,
     current_session_id: str = None,
+    current_session_key: str = None,
 ) -> str:
     """Scroll shape: return a window of messages centered on an anchor.
 
@@ -349,6 +383,9 @@ def _scroll(
         session_meta = {}
     if not session_meta:
         return tool_error(f"session_id not found: {session_id}", success=False)
+    current_session_key = _normalize_session_key(current_session_key)
+    if current_session_key and str(session_meta.get("session_key") or "").strip() != current_session_key:
+        return tool_error(f"session_id not found: {session_id}", success=False)
 
     # Fetch the window
     try:
@@ -379,7 +416,7 @@ def _scroll(
         if owning and owning != session_id:
             a_root = _resolve_to_parent(db, session_id)
             o_root = _resolve_to_parent(db, owning)
-            if a_root and o_root and a_root == o_root:
+            if a_root and o_root and a_root == o_root and _session_visible_for_key(db, owning, current_session_key):
                 try:
                     rebind_view = db.get_messages_around(owning, around_message_id, window=window)
                     messages = rebind_view.get("window") or []
@@ -433,21 +470,35 @@ def _title_match_result(
     db,
     query: str,
     current_lineage_root: Optional[str],
+    current_session_key: str = None,
 ) -> Optional[Dict[str, Any]]:
     """Return a discovery-shaped result when the query matches a session title."""
     title_query = _normalize_title_query(query)
     if not title_query:
         return None
 
+    current_session_key = _normalize_session_key(current_session_key)
     try:
-        session_id = db.resolve_session_by_title(title_query)
+        try:
+            session_id = db.resolve_session_by_title(
+                title_query,
+                session_key=current_session_key,
+            )
+        except TypeError:
+            # Compatibility with lightweight test/third-party SessionDB fakes;
+            # the metadata visibility checks below remain fail-closed.
+            session_id = db.resolve_session_by_title(title_query)
     except Exception:
         logging.debug("resolve_session_by_title failed for %r", title_query, exc_info=True)
         return None
     if not session_id:
         return None
+    if not _session_visible_for_key(db, session_id, current_session_key):
+        return None
 
     lineage_root = _resolve_to_parent(db, session_id)
+    if not _session_visible_for_key(db, lineage_root, current_session_key):
+        return None
     if current_lineage_root and lineage_root == current_lineage_root:
         return None
 
@@ -503,11 +554,17 @@ def _discover(
     limit: int,
     sort: Optional[str],
     current_session_id: str = None,
+    current_session_key: str = None,
 ) -> str:
     """Discovery shape: FTS5 + anchored window + bookends per hit. Single call."""
     role_list = role_filter if role_filter else ["user", "assistant"]
     current_lineage_root = _resolve_to_parent(db, current_session_id) if current_session_id else None
-    title_result = _title_match_result(db, query, current_lineage_root)
+    title_result = _title_match_result(
+        db,
+        query,
+        current_lineage_root,
+        current_session_key=current_session_key,
+    )
 
     try:
         raw_results = db.search_messages(
@@ -519,6 +576,7 @@ def _discover(
             # of cron rows are still in hand for the demotion pass below.
             offset=0,
             sort=sort,
+            session_key=_normalize_session_key(current_session_key),
         )
     except Exception as e:
         logging.error("FTS5 search failed: %s", e, exc_info=True)
@@ -622,6 +680,7 @@ def session_search(
     limit: int = 3,
     db=None,
     current_session_id: str = None,
+    current_session_key: str = None,
     # Scroll shape
     session_id: str = None,
     around_message_id: int = None,
@@ -651,6 +710,17 @@ def session_search(
             from hermes_state import format_session_db_unavailable
             return tool_error(format_session_db_unavailable(), success=False)
 
+    current_session_key = _normalize_session_key(current_session_key)
+
+    # A gateway-scoped agent may only inspect the durable session namespace
+    # assigned by the platform. Reject cross-profile selectors before profile
+    # lookup so the response cannot reveal whether another profile exists.
+    if current_session_key and profile is not None and str(profile).strip():
+        return tool_error(
+            "Cross-profile session access is unavailable in this scoped session.",
+            success=False,
+        )
+
     # Normalise a raw `@session:<profile>/<id>` link value passed as session_id.
     # Session ids never contain "/", so a slash unambiguously means profile/id —
     # always strip the prefix off the id, and adopt the embedded profile only
@@ -659,6 +729,11 @@ def session_search(
     if isinstance(session_id, str) and "/" in session_id:
         emb_profile, _, emb_id = session_id.partition("/")
         if emb_id:
+            if current_session_key and emb_profile:
+                return tool_error(
+                    "Cross-profile session access is unavailable in this scoped session.",
+                    success=False,
+                )
             session_id = emb_id
             if emb_profile and (profile is None or not str(profile).strip()):
                 profile = emb_profile
@@ -683,27 +758,29 @@ def session_search(
             around_message_id=around_message_id,
             window=window,
             current_session_id=current_session_id,
+            current_session_key=current_session_key,
         )
 
     # Read shape: a session_id with no anchor → dump the whole session.
     if isinstance(session_id, str) and session_id.strip():
         sid = session_id.strip()
-        result = _read_session(db, sid)
+        result = _read_session(db, sid, current_session_key=current_session_key)
         if json.loads(result).get("success"):
             return result
 
         # Miss in the target profile — the model may have dropped the owning
         # profile from the link. Scan every profile and read it from wherever
         # it lives, tagging the profile it was found in.
-        located, owner = _locate_session_db(sid)
-        if located is not None:
-            try:
-                found = json.loads(_read_session(located, sid))
-            finally:
-                located.close()
-            if found.get("success"):
-                found["profile"] = owner
-                return json.dumps(found, ensure_ascii=False)
+        if not current_session_key:
+            located, owner = _locate_session_db(sid)
+            if located is not None:
+                try:
+                    found = json.loads(_read_session(located, sid))
+                finally:
+                    located.close()
+                if found.get("success"):
+                    found["profile"] = owner
+                    return json.dumps(found, ensure_ascii=False)
         return result
 
     # Limit clamp [1, 10]
@@ -716,7 +793,7 @@ def session_search(
 
     # Browse shape: no query → recent sessions.
     if not query or not isinstance(query, str) or not query.strip():
-        return _list_recent_sessions(db, limit, current_session_id)
+        return _list_recent_sessions(db, limit, current_session_id, current_session_key)
 
     # Parse role_filter
     role_list: Optional[List[str]] = None
@@ -737,6 +814,7 @@ def session_search(
         limit=limit,
         sort=sort_norm,
         current_session_id=current_session_id,
+        current_session_key=current_session_key,
     )
 
 
@@ -915,6 +993,7 @@ registry.register(
         profile=args.get("profile"),
         db=kw.get("db"),
         current_session_id=kw.get("current_session_id"),
+        current_session_key=kw.get("current_session_key"),
     ),
     check_fn=check_session_search_requirements,
     emoji="🔍",
diff --git a/tools/terminal_tool.py b/tools/terminal_tool.py
index 44ef03af7..7b0640d3f 100644
--- a/tools/terminal_tool.py
+++ b/tools/terminal_tool.py
@@ -32,6 +32,7 @@ Usage:
 """
 
 import importlib.util
+import hashlib
 import json
 import logging
 import os
@@ -202,6 +203,18 @@ def set_approval_callback(cb):
 
 def _get_sudo_password_cache_scope() -> str:
     """Return the cache scope for interactive sudo passwords."""
+    # Approval identity is intentionally per API run, even when concurrent
+    # runs share a durable gateway/session scope for memory and sandboxes.
+    # Prefer it so one run can never reuse another run's sudo credential.
+    try:
+        from tools.approval import get_current_session_key
+
+        approval_session_key = get_current_session_key(default="")
+    except Exception:
+        approval_session_key = ""
+    if approval_session_key:
+        return f"approval:{approval_session_key}"
+
     try:
         from gateway.session_context import get_session_env
 
@@ -1120,17 +1133,52 @@ def clear_task_env_overrides(task_id: str):
     _task_env_overrides.pop(task_id, None)
 
 
-def _resolve_container_task_id(task_id: Optional[str]) -> str:
+def _session_scoped_container_task_id(agent_scope_key: Optional[str] = None) -> str:
+    """Return a stable terminal sandbox key for the current gateway session."""
+    session_key = str(agent_scope_key or "").strip()
+    if not session_key:
+        try:
+            from gateway.session_context import get_session_env
+
+            session_key = str(get_session_env("HERMES_SESSION_KEY", "") or "").strip()
+        except Exception:
+            session_key = ""
+    if not session_key:
+        return ""
+
+    digest = hashlib.sha256(session_key.encode("utf-8")).hexdigest()[:12]
+    safe = re.sub(r"[^A-Za-z0-9_.-]+", "-", session_key).strip(".-") or "agent"
+    if len(safe) > 80:
+        safe = safe[:80].rstrip(".-") or "agent"
+    return f"gateway-{safe}-{digest}"
+
+
+def _is_session_scoped_container_task_id(task_id: Optional[str]) -> bool:
+    """Return whether *task_id* is an already-resolved gateway sandbox key."""
+    return bool(
+        task_id
+        and re.fullmatch(r"gateway-.+-[0-9a-f]{12}", str(task_id))
+    )
+
+
+def _resolve_container_task_id(
+    task_id: Optional[str],
+    agent_scope_key: Optional[str] = None,
+) -> str:
     """
     Map a tool-call ``task_id`` to the container/sandbox key used by
     ``_active_environments``.
 
-    The top-level agent passes ``task_id=None`` and lands on ``"default"``.
-    ``delegate_task`` children pass their own subagent ID so that
-    file-state tracking, the active-subagents registry, and TUI events stay
-    distinct per child -- but we deliberately collapse that ID back to
-    ``"default"`` here so subagents share the parent's long-lived container
-    (one bash, one /workspace, one set of installed packages).
+    Gateway/API sessions that provide ``HERMES_SESSION_KEY`` get a container
+    key derived from that stable agent scope, so private/channel agents do not
+    share live cwd, background processes, or sandbox state.
+    ``delegate_task`` children inherit the same context var and therefore
+    share their parent agent's workspace, while their own task_id still keeps
+    file-state tracking and TUI events distinct.
+
+    Without a gateway session key, the top-level agent passes ``task_id=None``
+    and lands on ``"default"``. Ordinary task ids also collapse to
+    ``"default"`` to preserve the CLI/shared-container contract.
 
     Exception: RL / benchmark environments (TerminalBench2, HermesSweEnv, ...)
     call ``register_task_env_overrides(task_id, {...})`` to request a
@@ -1152,6 +1200,9 @@ def _resolve_container_task_id(task_id: Optional[str]) -> str:
         overrides = _task_env_overrides[task_id]
         if set(overrides.keys()) & _ISOLATION_KEYS:
             return task_id
+    session_task_id = _session_scoped_container_task_id(agent_scope_key)
+    if session_task_id:
+        return session_task_id
     return "default"
 
 
@@ -1204,6 +1255,14 @@ def _safe_getcwd() -> str:
     try:
         return os.getcwd()
     except FileNotFoundError:
+        try:
+            from agent.runtime_cwd import resolve_context_cwd
+
+            context_cwd = resolve_context_cwd()
+            if context_cwd and context_cwd.is_dir():
+                return str(context_cwd)
+        except Exception:
+            pass
         return os.getenv("TERMINAL_CWD") or os.path.expanduser("~")
 
 
@@ -1297,16 +1356,23 @@ def _get_env_config() -> Dict[str, Any]:
     else:
         default_cwd = "/root"
 
-    # Read TERMINAL_CWD but sanity-check it for container backends.
+    try:
+        from agent.runtime_cwd import _session_cwd_override
+
+        raw_cwd = _session_cwd_override()
+    except Exception:
+        raw_cwd = ""
+
+    # Read the session cwd but sanity-check it for container backends.
     # If Docker cwd passthrough is explicitly enabled, remap the host path to
     # /workspace and track the original host path separately. Otherwise keep the
     # normal sandbox behavior and discard host paths.
-    cwd = os.getenv("TERMINAL_CWD", default_cwd)
+    cwd = raw_cwd or os.getenv("TERMINAL_CWD", default_cwd)
     if cwd and not _is_ssh_remote_tilde_cwd(env_type, cwd):
         cwd = os.path.expanduser(cwd)
     host_cwd = None
     if env_type == "docker" and mount_docker_cwd:
-        docker_cwd_source = os.getenv("TERMINAL_CWD") or _safe_getcwd()
+        docker_cwd_source = raw_cwd or _safe_getcwd()
         candidate = os.path.abspath(os.path.expanduser(docker_cwd_source))
         if (
             any(candidate.startswith(p) for p in _HOST_CWD_PREFIXES)
@@ -1640,6 +1706,11 @@ def _stop_cleanup_thread():
 
 def get_active_env(task_id: str):
     """Return the active BaseEnvironment for *task_id*, or None."""
+    # Resolved gateway keys must never fall through to an unrelated unscoped
+    # `default` environment when the scoped environment has already gone away.
+    if _is_session_scoped_container_task_id(task_id):
+        with _env_lock:
+            return _active_environments.get(task_id)
     lookup = _resolve_container_task_id(task_id)
     with _env_lock:
         return _active_environments.get(lookup) or _active_environments.get(task_id)
@@ -1716,18 +1787,31 @@ def cleanup_vm(task_id: str, *, force_remove: bool = False):
     # actual (potentially slow) env.cleanup() call to outside the lock
     # so other tool calls aren't blocked.
     env = None
+    raw_task_id = task_id or ""
     with _env_lock:
-        env = _active_environments.pop(task_id, None)
-        _last_activity.pop(task_id, None)
+        resolved_task_id = raw_task_id
+        if (
+            resolved_task_id not in _active_environments
+            and not _is_session_scoped_container_task_id(resolved_task_id)
+        ):
+            resolved_task_id = _resolve_container_task_id(raw_task_id)
+        env = _active_environments.pop(resolved_task_id, None)
+        _last_activity.pop(resolved_task_id, None)
+        if raw_task_id != resolved_task_id:
+            _last_activity.pop(raw_task_id, None)
 
     # Clean up per-task creation lock
     with _creation_locks_lock:
-        _creation_locks.pop(task_id, None)
+        _creation_locks.pop(resolved_task_id, None)
+        if raw_task_id != resolved_task_id:
+            _creation_locks.pop(raw_task_id, None)
 
     # Invalidate stale file_ops cache entry
     try:
         from tools.file_tools import clear_file_ops_cache
-        clear_file_ops_cache(task_id)
+        clear_file_ops_cache(resolved_task_id)
+        if raw_task_id != resolved_task_id:
+            clear_file_ops_cache(raw_task_id)
     except ImportError:
         pass
 
@@ -1749,14 +1833,14 @@ def cleanup_vm(task_id: str, *, force_remove: bool = False):
         elif hasattr(env, 'terminate'):
             env.terminate()
 
-        logger.info("Manually cleaned up environment for task: %s", task_id)
+        logger.info("Manually cleaned up environment for task: %s", resolved_task_id)
 
     except Exception as e:
         error_str = str(e)
         if "404" in error_str or "not found" in error_str.lower():
-            logger.info("Environment for task %s already cleaned up", task_id)
+            logger.info("Environment for task %s already cleaned up", resolved_task_id)
         else:
-            logger.warning("Error cleaning up environment for task %s: %s", task_id, e)
+            logger.warning("Error cleaning up environment for task %s: %s", resolved_task_id, e)
 
 
 def _atexit_cleanup():
@@ -2018,6 +2102,7 @@ def terminal_tool(
     pty: bool = False,
     notify_on_complete: bool = False,
     watch_patterns: Optional[List[str]] = None,
+    agent_scope_key: Optional[str] = None,
 ) -> str:
     """
     Execute a command in the configured terminal environment.
@@ -2071,7 +2156,7 @@ def terminal_tool(
         # task_ids collapse back to "default" so the top-level agent and
         # every delegate_task child share one container; only task_ids with
         # a registered env override (RL benchmarks) get isolated sandboxes.
-        effective_task_id = _resolve_container_task_id(task_id)
+        effective_task_id = _resolve_container_task_id(task_id, agent_scope_key)
 
         # Check per-task overrides (set by environments like TerminalBench2Env)
         # before falling back to global env var config. ``resolve_task_overrides``
@@ -3015,6 +3100,7 @@ def _handle_terminal(args, **kw):
         pty=args.get("pty", False),
         notify_on_complete=args.get("notify_on_complete", False),
         watch_patterns=args.get("watch_patterns"),
+        agent_scope_key=kw.get("agent_scope_key"),
     )
 
 
diff --git a/tools/browser_camofox_state.py b/tools/browser_camofox_state.py
--- a/tools/browser_camofox_state.py
+++ b/tools/browser_camofox_state.py
@@ -27,16 +27,21 @@ def get_camofox_identity(task_id: Optional[str] = None) -> Dict[str, str]:
     """Return the stable Hermes-managed Camofox identity for this profile.
-
+
-    The user identity is profile-scoped (same Hermes profile = same userId).
-    The session key is scoped to the logical browser task so newly created
-    tabs within the same profile reuse the same identity contract.
+    Managed gateway sessions use their stable Agent scope for both identities,
+    so private/channel Agents never share cookies or Camofox session state.
     """
     scope_root = str(get_camofox_state_dir())
-    logical_scope = task_id or "default"
+    try:
+        from gateway.session_context import get_session_env
+
+        agent_scope = str(get_session_env("HERMES_SESSION_KEY", "") or "").strip()
+    except Exception:
+        agent_scope = ""
+    logical_scope = agent_scope or task_id or "default"
     user_digest = uuid.uuid5(
         uuid.NAMESPACE_URL,
-        f"camofox-user:{scope_root}",
+        f"camofox-user:{scope_root}:{logical_scope}",
     ).hex[:10]
     session_digest = uuid.uuid5(
         uuid.NAMESPACE_URL,
         f"camofox-session:{scope_root}:{logical_scope}",
     ).hex[:16]

diff --git a/gateway/relay/adapter.py b/gateway/relay/adapter.py
--- a/gateway/relay/adapter.py
+++ b/gateway/relay/adapter.py
@@ -74,11 +74,14 @@ class RelayAdapter(BasePlatformAdapter):
         # chat belongs to (Phase 1.5 multi-platform-per-agent). One relay adapter
         # fronts N platforms on one WS; an outbound reply must egress through the
         # platform the inbound came from. We remember it per chat_id from the
+        # same inbound event whose unguessable message ID binds every reply to
+        # the exact platform turn at the managed connector.
         # inbound event's source.platform and stamp it on the OutboundFrame so the
         # connector dispatches to the right sender. Empty for a single-platform
         # gateway (the connector falls back to its session default). See
         # _capture_scope / send.
         self._platform_by_chat: Dict[str, str] = {}
+        self._message_by_chat: Dict[str, str] = {}
         self.supports_code_blocks = descriptor.markdown_dialect not in ("", "plain")
         # Phase 7 Unit 7d-B: watches the transport for a terminal auth revocation
         # (a 4401 close after a successful handshake = the operator opted this
@@ -252,6 +255,10 @@ class RelayAdapter(BasePlatformAdapter):
             chat = getattr(src, "chat_id", None)
             if not chat:
                 return
+            message_id = getattr(src, "message_id", None) or getattr(event, "message_id", None)
+            if message_id:
+                self._message_by_chat[str(chat)] = str(message_id)
+
             # Phase 1.5: remember the underlying platform for this chat so the
             # reply egresses through the right sender (one relay adapter fronts N
             # platforms). source.platform is a Platform enum (e.g. Platform.DISCORD,
@@ -289,6 +296,10 @@ class RelayAdapter(BasePlatformAdapter):
         No-op when the relevant value is already present or unknown for this chat.
         """
         meta: Dict[str, Any] = dict(metadata or {})
+        if not meta.get("enterprise_turn_id"):
+            message_id = self._message_by_chat.get(str(chat_id))
+            if message_id:
+                meta["enterprise_turn_id"] = message_id
         if not meta.get("scope_id"):
             scope = self._scope_by_chat.get(str(chat_id))
             if scope:
diff --git a/gateway/session.py b/gateway/session.py
--- a/gateway/session.py
+++ b/gateway/session.py
@@ -12,6 +12,7 @@ import hashlib
 import logging
 import os
 import json
+import re
 import threading
 import uuid
 from pathlib import Path
@@ -176,6 +177,12 @@ class SessionSource:
     parent_chat_id: Optional[str] = None  # Parent channel when chat_id refers to a thread
     message_id: Optional[str] = None  # ID of the triggering message (for pin/reply/react)
     role_authorized: bool = False  # True when adapter granted access via role (not user ID)
+    # Internal platform-owned Agent scope. It is accepted only when the local
+    # relay transport also stamps delivered_via_upstream_relay, and is excluded
+    # from to_dict/from_dict so ordinary adapters and persisted peer data cannot
+    # forge a canonical runtime scope.
+    enterprise_session_key: Optional[str] = None
+
     # Profile this inbound message is routed to in a multiplexing gateway
     # (from the /p/<profile>/ URL prefix or per-credential adapter ownership).
     # None => the gateway's active/default profile. Drives both session-key
@@ -868,6 +875,28 @@ def _session_key_namespace(profile: Optional[str]) -> str:
     return f"agent:{profile}"
-
+
-
+
+_ENTERPRISE_AGENT_SCOPE_RE = re.compile(
+    r"(?:private:[1-9][0-9]*|channel:[A-Za-z0-9_.-]+:main-agent)"
+)
+
+
+def normalize_enterprise_agent_scope_key(value: object) -> Optional[str]:
+    """Return a valid platform-owned Agent scope key, otherwise None."""
+    if not isinstance(value, str):
+        return None
+    clean = value.strip()
+    return clean if _ENTERPRISE_AGENT_SCOPE_RE.fullmatch(clean) else None
+
+
+def enterprise_relay_session_key(source: SessionSource) -> Optional[str]:
+    """Return the authenticated relay scope that overrides native keying."""
+    if not getattr(source, "delivered_via_upstream_relay", False):
+        return None
+    return normalize_enterprise_agent_scope_key(
+        getattr(source, "enterprise_session_key", None)
+    )
+
+
 def build_session_key(
     source: SessionSource,
     group_sessions_per_user: bool = True,
@@ -904,2 +933,6 @@ def build_session_key(
     """
+    enterprise_key = enterprise_relay_session_key(source)
+    if enterprise_key:
+        return enterprise_key
+
     ns = _session_key_namespace(profile)
diff --git a/gateway/relay/ws_transport.py b/gateway/relay/ws_transport.py
--- a/gateway/relay/ws_transport.py
+++ b/gateway/relay/ws_transport.py
@@ -129,6 +129,10 @@ def _event_from_wire(raw: Dict[str, Any]) -> MessageEvent:
         # config/credential scope — the same field the /p/<profile>/ HTTP
         # prefix and per-credential polling adapters already set.
         profile=src.get("profile"),
+        # Platform-owned logical Agent scope. build_session_key accepts this
+        # only together with delivered_via_upstream_relay, which is stamped
+        # locally below and cannot be supplied by an ordinary adapter.
+        enterprise_session_key=raw.get("enterprise_session_key"),
         # Authentic upstream-trust signal: this event arrived over the
         # per-instance-authenticated relay WS, so the connector already resolved
         # it to this instance's owner-bound author. ``platform`` is the
