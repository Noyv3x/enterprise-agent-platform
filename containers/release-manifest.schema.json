{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-platform.invalid/schemas/release-manifest-v1.json",
  "title": "Agent Platform release manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "channel",
    "source_commit",
    "generated_at",
    "protocol_version",
    "database_schema_version",
    "manager",
    "compose",
    "images"
  ],
  "properties": {
    "schema_version": { "const": 1 },
    "channel": { "const": "main" },
    "source_commit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "generated_at": { "type": "string", "format": "date-time" },
    "protocol_version": { "const": 1 },
    "database_schema_version": { "type": "integer", "minimum": 1 },
    "manager": { "$ref": "#/$defs/manager_release" },
    "compose": { "$ref": "#/$defs/artifact" },
    "images": {
      "type": "object",
      "minProperties": 11,
      "maxProperties": 11,
      "propertyNames": { "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$" },
      "additionalProperties": { "$ref": "#/$defs/image" },
      "required": [
        "platform",
        "agent-runtime",
        "camofox",
        "agent-sandbox",
        "searxng",
        "firecrawl-api",
        "firecrawl-playwright",
        "firecrawl-postgres",
        "firecrawl-redis",
        "firecrawl-rabbitmq",
        "handoff-fs-helper"
      ]
    },
    "namespace_handoff": { "$ref": "#/$defs/namespace_handoff" }
  },
  "$defs": {
    "artifact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url", "sha256"],
      "properties": {
        "url": { "type": "string", "pattern": "^https://" },
        "sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" }
      }
    },
    "manager_release": {
      "type": "object",
      "additionalProperties": false,
      "required": ["version", "artifacts"],
      "properties": {
        "version": { "type": "string", "minLength": 1 },
        "artifacts": {
          "type": "object",
          "additionalProperties": false,
          "required": ["amd64", "arm64"],
          "properties": {
            "amd64": { "$ref": "#/$defs/artifact" },
            "arm64": { "$ref": "#/$defs/artifact" }
          }
        }
      }
    },
    "handoff_manager_release": {
      "allOf": [
        { "$ref": "#/$defs/manager_release" },
        {
          "type": "object",
          "properties": {
            "version": {
              "type": "string",
              "pattern": "^[0-9a-f]{40}$"
            }
          }
        }
      ]
    },
    "source_namespace_binding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["profile_id", "manager", "compose"],
      "properties": {
        "profile_id": { "const": "ubitech-agent-v1" },
        "manager": { "$ref": "#/$defs/handoff_manager_release" },
        "compose": { "$ref": "#/$defs/artifact" }
      }
    },
    "target_namespace_binding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["profile_id", "manager", "compose"],
      "properties": {
        "profile_id": { "const": "agent-platform-v1" },
        "manager": { "$ref": "#/$defs/handoff_manager_release" },
        "compose": { "$ref": "#/$defs/artifact" }
      }
    },
    "namespace_handoff": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_version",
        "predecessor_generation",
        "bridge_generation",
        "source",
        "target"
      ],
      "properties": {
        "schema_version": { "const": 1 },
        "predecessor_generation": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$"
        },
        "bridge_generation": {
          "type": "string",
          "pattern": "^[0-9a-f]{40}$"
        },
        "source": { "$ref": "#/$defs/source_namespace_binding" },
        "target": { "$ref": "#/$defs/target_namespace_binding" }
      }
    },
    "image": {
      "type": "string",
      "pattern": "^[^@\\s]+@sha256:[0-9a-f]{64}$"
    }
  }
}
