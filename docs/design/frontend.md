# 前端设计

本文定义浏览器界面的结构和交互不变量。产品行为见[产品设计](product.md)，后端与 Runtime 关系见[系统架构](system-architecture.md)。

## 技术与发布边界

前端使用 React、TypeScript、Vite 和 Ant Design 6。可编辑源代码位于 `enterprise-agent-platform/frontend/`；`enterprise_agent_platform/static/` 是构建产物，只能通过前端构建脚本生成。

构建脚本在同一文件系统的暂存目录中完成 Vite 构建、资源完整性验证、压缩和原子发布。带 hash 的依赖先安装，`index.html` 最后提交；同时保留上一版必要入口资源，降低慢链路和更新切换时的缓存错配。

## 组件与视觉系统

Ant Design 是界面组件和交互语义的基础，负责按钮、菜单、表格、表单控件、状态、空态、加载态和响应式数据展示。平台只通过 `ConfigProvider` 配置品牌色、字号、圆角、密度、深浅主题与组件 Token，不复制组件库内部 DOM 或重新实现同类基础控件。现有业务 i18n 仍是产品文案的唯一来源；`zh-CN`、`en`、`zh-TW` 只映射到对应的 Ant Design 组件语言包。

组件库只随使用它的延迟路由加载，不能进入聊天首屏入口；管理面板继续按功能子页拆分，首次进入只加载管理骨架和当前页。构建产物不能为了缓存而强制聚合未使用的组件模块。

旧 `.btn`、`.card`、原生表单和功能级 CSS 允许在尚未迁移的页面中继续运行，但不得作为新界面的基础。迁移后的区域使用独立的 `eap-admin-*` 布局类承载产品结构，不以高优先级选择器覆盖 Ant Design 内部节点。应用主题上下文仍是深浅主题的唯一状态源，并映射为组件库的默认/暗色算法；管理界面采用紧凑密度，但触屏目标不得低于 44px。

管理面板作为一个完整迁移边界：导航、页头、账户、运行配置、更新、安全、Telegram、审计、OAuth、密钥和 Token 页面中的按钮、输入、选择、开关、状态与数据表必须使用同一组件语义。`CardHead`、指标块等承载业务信息层级的组合组件可以保留，但不得自行重做基础交互。Cognee 的 changed-only 动态配置收集器依赖真实原生字段及其 DOM 数据属性，是当前唯一允许保留原生控件的管理表单；其外层布局和操作仍须与管理设计系统一致。

## 应用外壳

Provider 层次必须保持以下职责：

1. i18n 和主题提供全局展示上下文。
2. `UpdateGate` 在登录、Store、Toast 和应用错误边界之外处理强制维护。
3. Error Boundary 隔离业务界面错误并提供安全重载。
4. Store 只在正常产品生命周期中创建。

导航、内容区、聊天预览侧栏和移动端遮罩由 App Shell 管理。路由是客户端视图状态，不引入第二套 URL 授权模型；每个数据请求仍由服务端鉴权。

## 状态与数据访问

前端使用自有 external store 和 `useSyncExternalStore`。Selector 必须返回稳定引用；不得在 selector 内直接创建新的 `[]`、`{}`、`map()` 或 `filter()` 结果。派生默认值应在 selector 外使用模块级常量或 memo 化处理。

所有普通 HTTP 请求通过统一 `api()` 入口：

- 同源 Cookie 自动携带；
- 401 统一使当前会话失效；
- 维护响应立即触发 `UpdateGate`；
- 账号切换会取消旧账号未完成请求并使用 generation 拒绝迟到结果；
- 普通请求、SSE 和后台轮询采用不同生命周期，各自的恢复边界由前端请求实现与测试约束；它们不复用 Agent Runtime 的空闲或 terminal 超时。

初始登录态通过紧凑 bootstrap 一次获取用户、频道、活动 scope、消息和 Agent 状态。后续资源按视图惰性加载；频道切换和 Agent 状态读取必须使用 scope/version fence，防止旧响应覆盖新界面。

用户显示名、职位、时区和频道名由界面编辑，但不因出现在 Agent 上下文中而成为高优先级指令。后端必须把这些可变字段序列化到闭合的不可信数据块；前端校验只负责交互与长度，不是提示词安全边界。

## 实时对话

聊天使用 scope SSE 获取消息、输入状态、工具过程、审批和最终输出，轮询仅作为恢复补充。乐观消息必须带本地身份，并与服务端消息按稳定游标合并。

工作记录只反映可见工具事件：

- 没有工具调用时不显示工作记录；
- 工具开始后，展开的工作过程固定在当前回复顶部；
- 最终输出开始出现后自动折叠工作过程；
- terminal 必须展示经服务端安全处理后的完整命令参数，不能只显示工具名；
- 重复、匿名或内部工具噪声应合并，审批状态不得重复显示。

上下文详细信息入口只展示最新已完成回复的上下文占用，不混入供应商、内部 Runtime 或其它诊断信息。

## 预览侧栏

浏览器和终端预览都是聊天页面的只读侧栏：

- 无打开浏览器 tab 时不显示浏览器按钮；
- 无运行中终端时不显示终端按钮；
- 资源状态存在但帧或日志尚未到达时显示骨架/加载态；
- 只有服务端确认资源不存在时才显示空状态；
- 浏览器以低刷新率展示当前 viewport，终端按多个运行进程分组展示；
- 预览不可发送按键、鼠标或终端输入。

## 响应式布局

界面必须以动态视口和安全区为约束，不依赖固定桌面宽度。手机上应用外壳、聊天内容、消息气泡、代码块、Composer 和侧栏都必须限制在可见宽度内；长命令、URL、表格和 pre 元素应内部滚动或安全换行，不能扩大根布局。

输入区固定在当前可用视口底部，但不得遮挡消息尾部。“跳到最新”按钮要避开输入区及系统安全区。桌面侧栏展开不能压缩内容到低于组件最小可读宽度。

管理面板是应用主侧栏内的二级布局，不能只根据浏览器 viewport 决定内部列数。只有管理内容容器至少可同时容纳 216px 二级菜单和 840px 正文时才显示常驻菜单，否则切换为紧凑分组选择器。页面标题、说明、主操作和刷新入口必须位于同一页头；一个页面最多保留一个高强调主操作，不能在正文卡片内重复标题和操作层级。

账号等结构化数据在宽屏使用紧凑表格，状态必须同时显示文字和颜色，危险或低频动作收入下拉菜单；窄屏切换为无需横向滚动的列表。账号行桌面高度保持在 64–72px，操作不可换行撑高整行。创建和编辑使用抽屉，手机上占满可用宽度。配置表单、审计工具、会话审计和统计区根据管理页自身的容器宽度降列；宽表格和趋势图只允许在自身区域内滚动，不能扩大页面根布局。

## i18n 与可访问性

界面支持 `zh-CN`、`en`、`zh-TW`，默认根据浏览器语言选择，并保存到 `eap-locale`。翻译 key 是界面契约；新增可见文字必须同时补齐三种语言。用户消息、Agent 输出、工具结果与文件不翻译。

对话框需要焦点圈定、Esc 关闭和嵌套栈管理；图标按钮必须有可读名称；状态不能只依赖颜色。主题初始化脚本在 React 启动前应用用户选择，避免首屏闪烁。

## 验证

组件测试应使用真实 Provider、Store 和请求边界覆盖关键生命周期，特别是空数据 selector、迟到响应、移动宽度、维护切换、SSE 重连和首帧加载。命令见[测试与验证](../development/testing.md)。
