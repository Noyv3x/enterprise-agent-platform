{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-platform.invalid/contracts/release-transition-receipt.schema.json",
  "title": "Release transition deployment receipt",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "transition_id",
    "challenge_id",
    "nonce",
    "receipt_type",
    "deployment_id",
    "key_id",
    "predecessor_generation",
    "candidate_generation",
    "observed_generation",
    "profile_id",
    "capability",
    "status",
    "architecture",
    "manager_sha256",
    "evidence_sha256",
    "issued_at",
    "expires_at"
  ],
  "properties": {
    "schema_version": { "const": 1 },
    "transition_id": { "const": "technical-namespace-v1" },
    "challenge_id": { "type": "string", "pattern": "^challenge_[0-9a-f]{32}$" },
    "nonce": { "type": "string", "pattern": "^[A-Za-z0-9_-]{43}$" },
    "receipt_type": {
      "enum": ["source_owner_ready", "target_handoff_committed"]
    },
    "deployment_id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9_-]{0,63}$" },
    "key_id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9_-]{0,63}$" },
    "predecessor_generation": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "candidate_generation": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "observed_generation": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "profile_id": { "enum": ["ubitech-agent-v1", "agent-platform-v1"] },
    "capability": { "enum": ["source_owner", "target_owner"] },
    "status": { "enum": ["idle", "committed"] },
    "architecture": { "enum": ["amd64", "arm64"] },
    "manager_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "evidence_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "issued_at": { "type": "string", "format": "date-time" },
    "expires_at": { "type": "string", "format": "date-time" }
  }
}
