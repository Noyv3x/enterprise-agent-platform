# 工作准则

- 开始改动前先阅读 canonical 文档层；设计、契约或行为变化必须先修改文档，再同步实现与测试。
- 不在本文件复制代码库结构、配置值、产品设计或运维事实；这些内容只属于 canonical 文档层。
- 只修改任务所需范围，保留用户和其他协作者的无关改动，不用破坏性命令掩盖冲突。
- 对外部输入、凭据和生产数据保持最小权限；高风险或不可逆操作必须先验证边界并提供恢复路径。
- 实现后运行与风险相称的格式、静态检查、测试和文档同步检查；不能用放宽断言替代修复。
- 临时文件应放在明确的临时位置并在完成后清理；提交中不保留诊断补丁、一次性迁移入口或无消费者兼容层。
